Primeros pasos en git

git version // te da la version de git
git init // primer paso para el repositorio add
git config --global --add safe.directory /home/jose/git/primerospasos  // agrega el directorio com seguro
git status -s
git add // agrega el contenido al repositorio
git commit -m "Agrega un mensaje al commit"
hola
